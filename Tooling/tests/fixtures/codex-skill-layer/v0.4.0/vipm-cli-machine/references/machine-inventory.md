# Machine Inventory (VIPM CLI)

## Verified Tools
- `vipm` available on `PATH`.
- Verified version:
  - `vipm 2026.1.0`

## Assumed Runtime Context
- Windows host runner.
- LabVIEW version defaults derive from nearest `.lvversion` unless overridden.
- Default helper bitness: `64`.
- Default helper command timeout: `120` seconds.

## Relevant Paths
- Skill root:
  - `C:\Users\Sergio Velderrain\.codex\skills\vipm-cli-machine`
- Helper output default:
  - `./TestResults/agent-logs/vipm-cli-machine.latest.json`
